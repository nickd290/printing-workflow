[phases.setup]
nixPkgs = ["nodejs", "pnpm"]

[phases.install]
cmds = ["cd ../.. && pnpm install"]

[phases.build]
cmds = ["cd ../.. && pnpm db:generate && pnpm build --filter=@printing-workflow/web"]

[start]
cmd = "pnpm start"
